[web]
%{ for instance in webservers ~}
${instance.name} ansible_host=${instance.external_ip}
%{ endfor ~}

[db]
%{ for instance in databases ~}
${instance.name} ansible_host=${instance.external_ip}
%{ endfor ~}

[storage]
%{ for instance in storage ~}
${instance.name} ansible_host=${instance.external_ip}
%{ endfor ~}
